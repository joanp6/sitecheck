// The audit and watch commands are not wired up yet. The core library is built and
// tested on its own; this project exists so the dependency direction is fixed from
// the start: the console front end depends on SiteCheck.Core, never the other way.
Console.WriteLine("sitecheck: no commands available yet.");
